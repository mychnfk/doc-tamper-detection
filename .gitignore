# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
venv/
.venv/
env/

# Model weights
*.pth
*.pt
*.ckpt
*.bin
*.safetensors
weights/
pretrained_models/
checkpoints/

# Data
data/
datasets/
*.pkl
*.h5

# Test images (may contain sensitive documents)
test_images/
samples/
example-images/
# eval-images 评测图含真实业务单据，公开仓库严禁入库（脚手架 README/.gitkeep 已跟踪不受影响）
eval-images/**/*.jpg
eval-images/**/*.jpeg
eval-images/**/*.png
eval-images/**/*.heic
eval-images/**/*.webp
eval-images/**/*.heif
eval-images/**/*.tif
eval-images/**/*.tiff
eval-images/**/*.bmp
eval-images/**/*.pdf

# 检测记录（含真实业务单据原图，严禁入库）
runs/

# 前端构建产物
web/node_modules/
web/dist/

# TruFor (external dependency, clone separately)
TruFor/

# HiFi-Net (external dependency, clone separately; CUDA-only source, 见 docs/dev-log.md Task 11)
HiFi_IFDL/
hifi_weights/

# Inference output
output/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Docker
docker-compose.override.yml

# Gradio
flagged/

# Jupyter
.ipynb_checkpoints/

# Env
.env
.env.local
.superpowers/
web/test-results/

# 部署基线图（真实业务单据，只进离线包不进仓库）
deploy/baseline/
# 打包产物
dist-package/
